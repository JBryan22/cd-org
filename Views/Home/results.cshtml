@if (@Model.Count == 0)
{
  <h2>There are no artists that fit the bill</h2>
}

@foreach (var entry in @Model)
{
  <h2>@entry.Key</h2>
  <ul>
    @foreach(var cd in entry.Value)
    {
      <li>@cd.GetTitle()</li>
    }
  </ul>
}

<p><a href="/">Back to Home</a></p>
